import {defaultDelugeConfig} from "@/interfaces/btclients/deluge";
import {defaultQbittorrentConfig} from "@/interfaces/btclients/qbittorrent";
import {defaultTransmissionConfig} from "@/interfaces/btclients/transmission";

export const clientsConfig = {
    deluge: defaultDelugeConfig,
    qbittorrent: defaultQbittorrentConfig,
    transmission: defaultTransmissionConfig
}