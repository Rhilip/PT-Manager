import {defaultDelugeConfig} from "@/common/interfaces/btclients/deluge";
import {defaultQbittorrentConfig} from "@/common/interfaces/btclients/qbittorrent";
import {defaultTransmissionConfig} from "@/common/interfaces/btclients/transmission";

export const clientsConfig = {
    deluge: defaultDelugeConfig,
    qbittorrent: defaultQbittorrentConfig,
    transmission: defaultTransmissionConfig
}